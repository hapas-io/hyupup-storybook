import * as theme from './theme';

const themes = [
  {
    name: 'Light(default)',
    color: theme.default.color,
  },
  {
    name: 'Dark',
    color: theme.DarkTheme.color,
  },
];

export default themes;
